# 角色-功能-授权表

> 数据来源：客户端 `client/src/router/index.tsx` 的 `APP_NAV`（侧边栏模块）与 `ROUTE_CODES`（页面授权码）；角色-权限分配以 `server/src/db/seeds/001_seed_data.ts` 为准；角色授予范围策略见下文「角色授予策略」（BUG-09，代码在 `server/src/routes/users.ts` / `auth.ts`）。
> 模块按**现场 UI 侧边栏分组**（系统管理 / 设计工作台 / 表单 / 审批 / 运维 / 通用）。
> 图例：✓ = 有权访问，✗ = 无权访问。标注「页面内操作」的为页面中的操作权限，**非独立侧边栏入口**。

| 模块 | 功能 | 授权码 | 管理员 | 设计者 | 填写者 | 审批者 | 运维 |
|---|---|---|---|---|---|---|---|
| 系统管理 | 用户管理 | `admin:manage_users` | ✓ | ✗ | ✗ | ✗ | ✗ |
| 系统管理 | 分配角色（页面内操作） | `admin:manage_users`（可授范围受限，见「角色授予策略」） | ✓ | ✗ | ✗ | ✗ | ✗ |
| 系统管理 | 角色管理 | `admin:manage_roles` | ✓ | ✗ | ✗ | ✗ | ✗ |
| 系统管理 | 数据管理 | `data:view` | ✓ | ✗ | ✗ | ✗ | ✓ |
| 系统管理 | 统计看板 | `data:view_stats` | ✓ | ✗ | ✗ | ✗ | ✓ |
| 系统管理 | 模板管理 | `template:publish` | ✓ | ✓ | ✗ | ✗ | ✗ |
| 系统管理 | 导出数据（页面内操作） | `data:export` | ✓ | ✗ | ✗ | ✗ | ✗ |
| 设计工作台 | 我的模板 | `template:edit` | ✓ | ✓ | ✗ | ✗ | ✗ |
| 设计工作台 | 草稿模板 | `template:edit` | ✓ | ✓ | ✗ | ✗ | ✗ |
| 设计工作台 | 创建表单 | `template:create` | ✓ | ✓ | ✗ | ✗ | ✗ |
| 设计工作台 | 删除模板（页面内操作） | `template:delete` | ✓ | ✓ | ✗ | ✗ | ✗ |
| 设计工作台 | 导出配置（页面内操作） | `template:export` | ✓ | ✓ | ✗ | ✗ | ✓ |
| 设计工作台 | 导入配置（页面内操作） | `template:import` | ✓ | ✓ | ✗ | ✗ | ✓ |
| 设计工作台 | 强制解锁（页面内操作） | `template:force_unlock` | ✓ | ✓ | ✗ | ✗ | ✗ |
| 表单 | 表单中心 | `form:fill` | ✓ | ✗ | ✓ | ✗ | ✗ |
| 表单 | 我的草稿 | `form:fill` | ✓ | ✗ | ✓ | ✗ | ✗ |
| 表单 | 我的提交 | `form:submit` | ✓ | ✗ | ✓ | ✗ | ✗ |
| 表单 | 撤回提交（页面内操作） | `form:withdraw` | ✓ | ✗ | ✓ | ✗ | ✗ |
| 审批 | 待审批 | `approval:view_pending` | ✓ | ✗ | ✗ | ✓ | ✗ |
| 审批 | 已审批 | `approval:view_pending` | ✓ | ✗ | ✗ | ✓ | ✗ |
| 审批 | 审批同意（页面内操作） | `approval:approve` | ✓ | ✗ | ✗ | ✓ | ✗ |
| 审批 | 审批拒绝（页面内操作） | `approval:reject` | ✓ | ✗ | ✗ | ✓ | ✗ |
| 审批 | 退回修改（页面内操作） | `approval:return` | ✓ | ✗ | ✗ | ✓ | ✗ |
| 审批 | 转交审批（页面内操作） | `approval:transfer` | ✓ | ✗ | ✗ | ✓ | ✗ |
| 运维 | 导入配置 | `template:import` | ✓ | ✓ | ✗ | ✗ | ✓ |
| 运维 | 迁移记录 | `data:view` | ✓ | ✗ | ✗ | ✗ | ✓ |
| 运维 | 模板查看 | `data:view` | ✓ | ✗ | ✗ | ✗ | ✓ |
| 通用 | 通知中心 | —（无授权码，始终可见） | ✓ | ✓ | ✓ | ✓ | ✓ |

**角色说明**：5 大角色 ↔ 5 大门户（管理员 | 设计者 | 填写者 | 审批者 | 运维）。「管理员」拥有全部 20 个权限码；其余角色按上表授权。「通用 / 通知中心」无需授权码，所有角色可见。

## 角色授予策略（BUG-09，2026-08-21 定稿）

**一句话**：禁止授予管理类（`admin:*`）角色，其余可授。

- **入口**：分配角色发生在「用户管理」页内（新建用户带初始角色 `POST /users`、替换角色 `POST /users/:id/roles`），入口门禁同为 `admin:manage_users`。
- **可授范围**：
  - 持 `admin:manage_roles`（完整角色管理员）→ **可授任意角色**（含「管理员」）。
  - 未持 `admin:manage_roles`（如仅持 `admin:manage_users` 的「有限权限的管理员」）→ **仅可授予不含任何管理类权限的角色**（权限码中无 `admin:*`），如「填写者」「审批者」；试图授予含 `admin:manage_users` / `admin:manage_roles` 等管理类权限的角色（如「管理员」）→ **403 `FORBIDDEN`**。
- **角色目录可见性**（`GET /roles`）：对持 `admin:manage_users` 者放开（便于「用户管理」页拉取选单），但非角色管理员只见**可授予集**——不含管理类权限的角色。
- **目的**：阻止低权限管理员通过角色分配自提权为「管理员」（垂直越权 / Broken Function Level Authorization），同时不阻断其正常下发普通业务角色（填写者等）。
- **取舍**：初始方案为「目标角色权限集 ⊆ 调用者权限集」子集校验，过严（有限管理员连自身角色权限之外的普通业务角色都无法授予），故改为「禁止管理类，其余可授」。
