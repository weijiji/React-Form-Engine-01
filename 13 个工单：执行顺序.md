
## `/implement` 的工作原理

### 核心流程

```
/implement → 读取工单 → TDD（红-绿-重构）→ Code Review → Git Commit
```

每个 `/implement` 调用会：
1. **读取**你指定的工单文件
2. **内部驱动 `/tdd`**：先写测试（红）→ 写最小实现让测试通过（绿）→ 重构优化（重构），一个切片一个切片地迭代
3. **运行 `/code-review`**：从两个维度审查 diff——代码规范 + 规格对齐
4. **提交代码**：通过审查后 commit

### 关键规则：一个工单 = 一个全新会话

> 每次 `/implement` 都应该在**干净的上下文窗口**中启动。上一个工单做完 → 清空上下文 → 下一个工单。

这是为了保持在 [Smart Zone](https://www.aihero.dev/ai-coding-dictionary/smart-zone)（~120k tokens）内，确保模型推理质量不衰减。

---

## 📊 你的 13 个工单：执行顺序

根据 `Blocked by` 字段，依赖关系如下：

```
第 1 批（零阻塞，可并行）
├── 01  项目脚手架 + 数据库          ← 基础设施
└── 02  表单引擎核心（纯逻辑）        ← 无外部依赖的纯函数模块

第 2 批
├── 03  表单渲染器 + 字段组件         ← 阻塞于 02
└── 09  认证 + RBAC 权限             ← 阻塞于 01

第 3 批
└── 04  模板 API + 表单设计器         ← 阻塞于 01 + 03

第 4 批（均阻塞于 04，可并行）
├── 05  表单提交 + 草稿 + 填写器
├── 08  条件编辑器 MVP
├── 10  审批链配置
└── 12  模板导出/导入

第 5 批
├── 06  审批引擎 + 审批操作 UI        ← 阻塞于 05
└── 11  数据管理 + Excel 导出         ← 阻塞于 05

第 6 批
└── 07  通知系统 SSE + 通知中心       ← 阻塞于 05 + 06

第 7 批
└── 13  安全加固 + 设计打磨           ← 阻塞于 09
```

---

## 🚀 实际操作方式

### 用法

在会话中输入：

```
/implement .scratch/form-engine-mvp/issues/01-project-scaffold-database.md
```

它会自动读取工单、按 checklist 逐项 TDD 实现、审查、提交。

### 推荐执行节奏

| 步骤 | 做什么 | 会话 |
|------|--------|------|
| 1 | `/implement` 工单 01 | 会话 A |
| 2 | `/implement` 工单 02 | 会话 B（可与 A 并行） |
| 3 | `/implement` 工单 03 | 新会话（01、02 完成后） |
| 4 | `/implement` 工单 09 | 新会话（01 完成后） |
| 5 | `/implement` 工单 04 | 新会话（01+03 完成后） |
| 6 | `/implement` 工单 05、08、10、12 | 各自独立会话（可并行） |
| ... | 依此类推，按依赖图逐层推进 | |

---