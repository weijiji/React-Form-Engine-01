{
  "name": "form-engine-core",
  "version": "0.1.0",
  "private": true,
  "description": "Dynamic Form Engine — pure-logic core (SchemaParser, ValidationEngine, ConditionEvaluator, VisibilityEngine, FormStateManager, ApprovalStateMachine, ApprovalResolver). Zero runtime dependencies.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vitest": "^2.1.9"
  }
}
