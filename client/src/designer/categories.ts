/** Template category choices — shared by blank creation (CreateBlankPage) and
    the designer's basic-info edit dialog (BUG-04). Mirrors the prototype. */
export const CATEGORIES = ["通用", "人力资源", "行政", "财务", "采购", "IT"];
